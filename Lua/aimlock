-- Date Updated -- 2:30 PM -- 12/7/2026 -- DD/MM/YYYY
-- loadstring(game:HttpGet(("https://raw.githubusercontent.com/Soulless268/Handmade_Scripts/refs/heads/main/Lua/aimlock"),true))()

local coregui = game:GetService("CoreGui")
local players = game:GetService("Players")
local rp = game:GetService("ReplicatedStorage")
local rs = game:GetService("RunService")
local uis = game:GetService("UserInputService")

local no_char_from_start = false

local plr = players.LocalPlayer
local char = plr.Character
local hum
local hrp
local plr_mouse = plr:GetMouse()
local cam = workspace.CurrentCamera

print("aimlock: trying to run module")

local gui_module = loadstring(game:HttpGet("https://raw.githubusercontent.com/Soulless268/Handmade_Scripts/refs/heads/main/Lua/Modules/GUI_Module"))()
local paths_module = loadstring(game:HttpGet("https://raw.githubusercontent.com/Soulless268/Handmade_Scripts/refs/heads/main/Lua/Modules/Paths"))()
local child_table = paths_module.child_table

if paths_module.place_register_plr_character then
	char = plr.Character or plr.CharacterAdded:Wait()
	hum = char:WaitForChild("Humanoid") :: Humanoid
	hrp = char:WaitForChild("HumanoidRootPart", 2) or char:WaitForChild("Root", 2) or char:FindFirstChildOfClass("Part") :: Part
else
	no_char_from_start = true
	print("nocharacter from the start")
end

print("aimlock: successful run module")

-----Settings-----
local Minimized = false
local DESTROYED = false
local targetenemy
local enable = false
local targetpart = "Head" --Head, HumanoidRootPart, Torso
local locknpc = true -- Lock Npc or not
local lockplayer = true -- Lock player or not
local lockteam = true
local show_highlight_plr_folder = false
local show_highlight_npc_folder = false
local predict_movement = true
local detect_through_wall = false
local lock_after_dead = false
local lock_nearest_enemy = false
local detectionRadius = 50
local totalTime_prediction = 10
local dt_prediction = 0.1
local ignore_players = {}
local frame_store = {}
local highlights_store = {}
local background_image_id = "rbxassetid://86093393173023"
local truecolor = Color3.fromRGB(170, 170, 0)
local falsecolor = Color3.fromRGB(163, 162, 165)
-----Settings-----

local runners = {
	runner_main = nil :: RBXScriptConnection,
	runner_lockon = nil :: RBXScriptConnection,
	runner_mouse = nil :: RBXScriptConnection,
	runner_input = nil :: RBXScriptConnection,
	runner_highlight_npc = nil :: RBXScriptConnection,
	runner_highlight_plr = nil :: RBXScriptConnection,
}

-----SetUp GUI-----
local screen = Instance.new("ScreenGui")
screen.Name = "NickAimLock"
screen.ResetOnSpawn = false
screen.Parent = coregui or plr.PlayerGui

local highlight_folder = Instance.new("Folder")
highlight_folder.Name = "HighlightsFolder"
highlight_folder.Parent = screen

local extrascreen

local main_frame : Frame
local catagories_frame : Frame
local ignore_players_frame : Frame
local aimlock_frame : Frame
local settings_frame : Frame

local show_button : TextButton
local close_button : TextButton
local minimize_button : TextButton

local selectpartframe
local ignore_players_scrollframe

local add_decent_button : TextButton
local remove_decent_button : TextButton

local selectpartbutton
local lockafterdeadbutton
local lock_nearest_enemy_button
local detectthroughwallbutton
local colorstatus
local radiustext
local aimlock_smoothness_text
local distancetext
local predictmovementbutton
local npclockbutton
local plrlockbutton
local teamlockbutton
local show_highlight_plr_folder_button
local show_highlight_npc_folder_button
local plr_folder_path
local npc_folder_path

local radius

--PreLoad Local Universals Function:
local function UiCorner(Parent, Ui)
	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = Ui
	Corner.Parent = Parent
	return Corner
end

local function UiStroke(Parent)
	local Stroke = Instance.new("UIStroke")
	Stroke.Color = Color3.fromRGB(255, 255, 255)
	Stroke.Thickness = 1
	Stroke.LineJoinMode = Enum.LineJoinMode.Miter
	Stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
	Stroke.Parent = Parent
	return Stroke
end

local function character_available()
	local available = false
	char = plr.Character
	if not char and not no_char_from_start then return available end
	if no_char_from_start then available = true return available end
	
	hrp = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Root") or char:FindFirstChildOfClass("Part") :: Part
	hum = char:FindFirstChildOfClass("Humanoid") :: Humanoid

	if hrp and hum then
		available = true
	end

	return available

end

local function sampleTrajectory(part, totalTime, dt)
	local samples = {}
	if dt <= 0 then return samples end
	local steps = math.floor(totalTime / dt)
	local pos0 = part.Position
	local v0 = part.AssemblyLinearVelocity
	local g = Vector3.new(0, -workspace.Gravity, 0)

	for i = 1, steps do
		local t = i * dt
		local pos = pos0 + v0 * t + 0.5 * g * t * t
		table.insert(samples, pos)
	end
	return samples
end

local function drawTrajectory(part)
	-- Example usage:
	-- local points = sampleTrajectory(somePart, 2, 0.05) -- positions for 0->2s every 0.05s
	local points = sampleTrajectory(part, totalTime_prediction, dt_prediction)
	-- remove old markers (simple approach)
	for _, p in ipairs(workspace:GetChildren()) do
		if p.Name == "TrajMarker" then p:Destroy() end
	end
	for i, pos in ipairs(points) do
		local m = Instance.new("Part")
		m.Size = Vector3.new(0.2,0.2,0.2)
		m.Anchored = true
		m.CanCollide = false
		m.Position = pos
		m.Name = "TrajMarker"
		m.Parent = workspace
	end
end

local function finding_(mousePos, children)
	local target_in_sight = {}
	for i,enemy in pairs(children) do
		if enemy == char then continue end
		if enemy:FindFirstChild(targetpart) then
			local enemypos = enemy[targetpart].CFrame.Position

			local screenPos, onScreen = cam:WorldToViewportPoint(enemypos)
			if onScreen then
				local dist = (Vector2.new(mousePos.X, mousePos.Y) - Vector2.new(screenPos.X, screenPos.Y)).Magnitude
				if dist <= detectionRadius then
					table.insert(target_in_sight, enemy)
				elseif targetenemy == enemy and dist >= detectionRadius then
					targetenemy = nil
				end
			elseif targetenemy == enemy and not onScreen then
				targetenemy = nil
			end
		end
	end

	if next(target_in_sight) == nil then return end

	local function get_enemy(enemy)
		local enemyPos = enemy[targetpart].CFrame.Position
		local playerPos = if char.PrimaryPart then char.PrimaryPart.CFrame.Position else hrp.CFrame.Position

		if not detect_through_wall then
			local ray_param = RaycastParams.new()
			ray_param.FilterType = Enum.RaycastFilterType.Exclude
			ray_param.FilterDescendantsInstances = {char, enemy}
			local raycast = workspace:Raycast(playerPos, enemyPos, ray_param)
			if raycast then warn("target have wall in bewtween | ", raycast.Instance) return end
		end

		local enemyplr = players:GetPlayerFromCharacter(enemy)
		if enemyplr and lockplayer then
			if not lockteam and enemyplr.Team == plr.Team then return end
			if table.find(ignore_players, enemyplr.Name) then return end
			targetenemy = enemy
			return targetenemy
		elseif not enemyplr and locknpc then
			targetenemy = enemy
			return targetenemy
		end
	end

	if lock_nearest_enemy then
		local nearest = nil
		local nearestDistance = math.huge

		for _,enemy in pairs(target_in_sight) do
			local enemyPos = enemy[targetpart].CFrame.Position
			local playerPos = if char.PrimaryPart then char.PrimaryPart.CFrame.Position else hrp.CFrame.Position
			local distance = (enemyPos - playerPos).Magnitude

			if distance < nearestDistance then
				nearestDistance = distance
				nearest = enemy
			end
		end

		if nearest then
			get_enemy(nearest)
		end

	else
		for _,enemy in pairs(target_in_sight) do
			if get_enemy(enemy) then break end
		end		
	end

	target_in_sight = {}

end

function lockon()
	if not targetenemy then return end
	local enemypos = targetenemy[targetpart].CFrame.Position
	local hum = targetenemy.Humanoid :: Humanoid
	local pos = cam.CFrame.Position

	if predict_movement and hum then
		local vel = targetenemy[targetpart].AssemblyLinearVelocity
		local predict_pos = enemypos + vel * dt_prediction
		cam.CFrame = CFrame.new(pos, predict_pos)
	else
		cam.CFrame = CFrame.new(pos, enemypos)
	end
end

function AimLock()
	local mousePos = uis:GetMouseLocation()

	local c_plr = paths_module.get_child_folder("plr_folder")
	local c_npc = paths_module.get_child_folder("npc_folder")

	if locknpc then
		finding_(mousePos, c_npc)
	end

	if lockplayer then
		finding_(mousePos, c_plr)
	end

end

local function getMousePosition()
	local mouse = plr_mouse
	local x, y = mouse.X, mouse.Y
	return x, y
end

do
	main_frame = Instance.new("Frame")
	main_frame.Draggable = true
	main_frame.Name = "Main"
	main_frame.Active = true
	main_frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
	main_frame.AnchorPoint = Vector2.new(1, 0)
	main_frame.Position = UDim2.new(1, 0, 0.15, 0)
	main_frame.Size = UDim2.new(0.25, 0, 0.4, 0)
	main_frame.Parent = screen
	UiStroke(main_frame)

	back_ground_frame = Instance.new("ImageLabel")
	back_ground_frame.BackgroundTransparency = 1
	back_ground_frame.ImageTransparency = .75
	back_ground_frame.Image = background_image_id
	back_ground_frame.Position = UDim2.new(0, 0, 0.1, 0)
	back_ground_frame.Size = UDim2.new(1,0,0.9,0)
	back_ground_frame.Parent = main_frame

	do

		close_button = Instance.new("TextButton")
		close_button.Text = "X"
		close_button.AnchorPoint = Vector2.new(1,0)
		close_button.BackgroundTransparency = 1
		close_button.TextScaled = true
		close_button.TextColor3 = Color3.fromRGB(255, 170, 0)
		close_button.Position = UDim2.new(1,0,0,0)
		close_button.Size = UDim2.new(0.05, 0,0.1, 0)
		close_button.Parent = main_frame

		minimize_button = Instance.new("TextButton")
		minimize_button.Text = "-"
		minimize_button.AnchorPoint = Vector2.new(1,0)
		minimize_button.BackgroundTransparency = 1
		minimize_button.TextScaled = true
		minimize_button.TextColor3 = Color3.fromRGB(255, 170, 0)
		minimize_button.Position = UDim2.new(.95,0,0,0)
		minimize_button.Size = UDim2.new(0.1,0,0.1,0)
		minimize_button.Parent = main_frame

		aimlock_frame = Instance.new("ScrollingFrame")
		aimlock_frame.Active = true
		aimlock_frame.Visible = false
		aimlock_frame.BackgroundTransparency = .25
		aimlock_frame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
		aimlock_frame.Position = UDim2.new(0.25, 0, 0.1, 0)
		aimlock_frame.Size = UDim2.new(.75,0,0.9,0)
		aimlock_frame.CanvasSize = UDim2.new(0,0,0,0)
		aimlock_frame.AutomaticCanvasSize = Enum.AutomaticSize.Y
		aimlock_frame.ScrollBarThickness = 0
		aimlock_frame.Parent = main_frame
		UiStroke(aimlock_frame)

		do
			local grid = Instance.new("UIGridLayout")
			grid.CellPadding = UDim2.new(0,0,0,1)
			grid.CellSize = UDim2.new(1,0,.15,0)
			grid.FillDirection = "Horizontal"
			grid.HorizontalAlignment = Enum.HorizontalAlignment.Center
			grid.VerticalAlignment = Enum.VerticalAlignment.Top
			grid.Parent = aimlock_frame

			colorstatus = gui_module.create_frame(aimlock_frame, "Status", nil, enable)

		end

		ignore_players_frame = Instance.new("ScrollingFrame")
		ignore_players_frame.Active = true
		ignore_players_frame.Visible = false
		ignore_players_frame.BackgroundTransparency = .25
		ignore_players_frame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
		ignore_players_frame.Position = UDim2.new(0.25, 0, 0.1, 0)
		ignore_players_frame.Size = UDim2.new(.75,0,0.9,0)
		ignore_players_frame.CanvasSize = UDim2.new(0,0,0,0)
		ignore_players_frame.AutomaticCanvasSize = Enum.AutomaticSize.Y
		ignore_players_frame.ScrollBarThickness = 0
		ignore_players_frame.Parent = main_frame
		UiStroke(ignore_players_frame)

		do
			local grid = Instance.new("UIGridLayout")
			grid.CellPadding = UDim2.new(0,0,0,1)
			grid.CellSize = UDim2.new(1,0,.15,0)
			grid.FillDirection = "Horizontal"
			grid.HorizontalAlignment = Enum.HorizontalAlignment.Center
			grid.VerticalAlignment = Enum.VerticalAlignment.Top
			grid.Parent = ignore_players_frame

		end

		settings_frame = Instance.new("ScrollingFrame")
		settings_frame.Active = true
		settings_frame.Visible = false
		settings_frame.BackgroundTransparency = .25
		settings_frame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
		settings_frame.Position = UDim2.new(0.25, 0, 0.1, 0)
		settings_frame.Size = UDim2.new(.75,0,0.9,0)
		settings_frame.CanvasSize = UDim2.new(0,0,0,0)
		settings_frame.AutomaticCanvasSize = Enum.AutomaticSize.Y
		settings_frame.ScrollBarThickness = 0
		settings_frame.Parent = main_frame
		UiStroke(settings_frame)

		do
			local grid = Instance.new("UIGridLayout")
			grid.CellPadding = UDim2.new(0,0,0,1)
			grid.CellSize = UDim2.new(1,0,.15,0)
			grid.FillDirection = "Horizontal"
			grid.HorizontalAlignment = Enum.HorizontalAlignment.Center
			grid.VerticalAlignment = Enum.VerticalAlignment.Top
			grid.Parent = settings_frame

			npclockbutton = gui_module.create_frame(settings_frame, "Lock Npc", "Lock Npc", locknpc)

			plrlockbutton = gui_module.create_frame(settings_frame, "Lock Player", "Lock Player", lockplayer)

			teamlockbutton = gui_module.create_frame(settings_frame, "Lock Team", "Lock Team", lockteam)

			selectpartbutton = gui_module.create_frame(settings_frame, "Select Part", "Select Part", "Click")

			predictmovementbutton = gui_module.create_frame(settings_frame, "Predict Movement", "Predict Movement", predict_movement)

			radiustext = gui_module.create_frame(settings_frame, "Radius", "Radius", detectionRadius)

			lockafterdeadbutton = gui_module.create_frame(settings_frame, "Lock after dead", "Lock after dead", lock_after_dead)

			detectthroughwallbutton = gui_module.create_frame(settings_frame, "Detect Through Wall", "e", detect_through_wall)

			lock_nearest_enemy_button = gui_module.create_frame(settings_frame, "Lock nearest enemy", "Lock nearest enemy to plr. Aim can change mid lock", lock_nearest_enemy)

			show_highlight_npc_folder_button, show_highlight_plr_folder_button = gui_module.create_frame(settings_frame, "Show Highlight", "Player Folder | NPC Folder", false, false)

			plr_folder_path = gui_module.create_frame(settings_frame, "Player Path", "Path", child_table.plr_folder.path:GetFullName())
			plr_folder_path.ClearTextOnFocus = false

			npc_folder_path = gui_module.create_frame(settings_frame, "NPC Path", "Path", child_table.npc_folder.path:GetFullName())
			npc_folder_path.ClearTextOnFocus = false

		end

		catagories_frame = Instance.new("ScrollingFrame")
		catagories_frame.Active = true
		catagories_frame.BackgroundTransparency = .25
		catagories_frame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
		catagories_frame.Position = UDim2.new(0, 0, 0.1, 0)
		catagories_frame.Size = UDim2.new(.25,0,0.9,0)
		catagories_frame.CanvasSize = UDim2.new(0,0,0,0)
		catagories_frame.AutomaticCanvasSize = Enum.AutomaticSize.Y
		catagories_frame.ScrollBarThickness = 0
		catagories_frame.Parent = main_frame
		UiStroke(catagories_frame)

		do
			local grid = Instance.new("UIGridLayout")
			grid.CellPadding = UDim2.new(0,0,0,1)
			grid.CellSize = UDim2.new(1,0,.15,0)
			grid.FillDirection = "Horizontal"
			grid.HorizontalAlignment = Enum.HorizontalAlignment.Center
			grid.VerticalAlignment = Enum.VerticalAlignment.Top
			grid.Parent = catagories_frame

			gui_module.current_catagory = settings_frame

			gui_module.create_button_catagory(catagories_frame, "AimLock", aimlock_frame)

			gui_module.create_button_catagory(catagories_frame, "Ignore Players", ignore_players_frame)

			gui_module.create_button_catagory(catagories_frame, "Settings", settings_frame)

		end

	end

	selectpartframe = Instance.new("Frame")
	selectpartframe.Draggable = true
	selectpartframe.Active = true
	selectpartframe.Visible = false
	selectpartframe.BackgroundColor3 = Color3.fromRGB(49, 42, 39)
	selectpartframe.Position = UDim2.new(0.25, 0, 0.15, 0)
	selectpartframe.Size = UDim2.new(.2,0,0.15,0)
	selectpartframe.Parent = screen

	do
		local grid = Instance.new("UIGridLayout")
		grid.CellPadding = UDim2.new(0,0,0,0)
		grid.CellSize = UDim2.new(.5,0,.4,0)
		grid.FillDirection = "Horizontal"
		grid.HorizontalAlignment = Enum.HorizontalAlignment.Center
		grid.VerticalAlignment = Enum.VerticalAlignment.Bottom
		grid.Parent = selectpartframe
	end

	extrascreen = Instance.new("ScreenGui")
	extrascreen.Name = "NickExtra_Aim"
	extrascreen.ResetOnSpawn = false
	extrascreen.Parent = coregui

	do
		radius = Instance.new("Frame")
		radius.Size = UDim2.new(0, detectionRadius*2, 0, detectionRadius*2)
		radius.BackgroundTransparency = .9
		radius.Parent = extrascreen

		do
			local e = Instance.new("UICorner")
			e.Parent = radius
			e.CornerRadius = UDim.new(1,0)

			local stroke = gui_module.UiStroke(radius)
			stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Contextual
			stroke.LineJoinMode = Enum.LineJoinMode.Round
		end
	end

	show_button = Instance.new("TextButton")
	show_button.Active = true
	show_button.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
	show_button.TextColor3 = Color3.fromRGB(255, 170, 0)
	show_button.Size = UDim2.new(0.05, 0, 0.085, 0)
	show_button.Text = "Show"
	show_button.Visible = false
	show_button.Parent = screen
	UiCorner(show_button, UDim.new(0.1,0))

end

-----Startup-----
local bodypart = {"Head", "Torso", "HumanoidRootPart"}
for i,v in pairs(bodypart) do
	local partbutton = Instance.new("TextButton")
	partbutton.Text = v
	partbutton.Parent = selectpartframe
	partbutton.Activated:Connect(function()
		selectpartframe.Visible = false
		targetpart = v
		selectpartbutton.Text = targetpart
	end)
end
selectpartbutton.Text = targetpart

local function create_plr_b(p)
	local partbutton = Instance.new("TextButton")
	partbutton.Text = p.Name
	partbutton.BackgroundColor3 = falsecolor
	partbutton.Parent = ignore_players_frame
	partbutton.Activated:Connect(function()
		if table.find(ignore_players, p.Name) then
			table.remove(ignore_players, table.find(ignore_players, p.Name))
			partbutton.BackgroundColor3 = falsecolor
			return
		else
			table.insert(ignore_players, p.Name)
			partbutton.BackgroundColor3 = truecolor
		end
	end)
end

for _,p in pairs(players:GetPlayers()) do
	if table.find(ignore_players, p.Name) then return end
	create_plr_b(p)
end

players.PlayerAdded:Connect(function(player)
	if table.find(ignore_players, player.Name) then return end
	create_plr_b(player)
end)

players.PlayerRemoving:Connect(function(player)
	if table.find(ignore_players, player.Name) then
		table.remove(ignore_players, table.find(ignore_players, player.Name))
	end
	local partbutton = ignore_players_frame:FindFirstChild(player.Name)
	if partbutton then
		partbutton:Destroy()
	end
end)

-----Startup-----

-----Function-----
close_button.Activated:Connect(function()

	for i,v in pairs(runners) do
		if v then
			v:Disconnect()
		end
	end

	enable = false
	DESTROYED = true

	screen:Destroy()
	extrascreen:Destroy()
end)

minimize_button.MouseButton1Click:Connect(function()
	Minimized = true
	main_frame.Visible = false
	show_button.Visible = true

	for _,v in pairs(screen:GetChildren()) do
		if v == main_frame then continue end
		if v == show_button then continue end
		if frame_store[v] then continue end
		if not v:IsA("Frame") then continue end
		frame_store[v] = v.Visible
		v.Visible = false
	end
end)

show_button.Activated:Connect(function()
	Minimized = false
	main_frame.Visible = true
	show_button.Visible = false

	for i,v in pairs(frame_store) do
		i.Visible = v
	end

	frame_store = {}
end)

radiustext.Changed:Connect(function()
	if typeof(tonumber(radiustext.Text)) == "number" then
		detectionRadius = tonumber(radiustext.Text)
		radius.Size = UDim2.new(0, detectionRadius*2, 0, detectionRadius*2)
	end
end)

	end
end)

selectpartbutton.Activated:Connect(function()
	selectpartframe.Visible = not selectpartframe.Visible
end)

plrlockbutton.Activated:Connect(function()
	lockplayer = not lockplayer
	plrlockbutton.BackgroundColor3 = if lockplayer then truecolor else falsecolor
end)

teamlockbutton.Activated:Connect(function()
	lockteam = not lockteam
	teamlockbutton.BackgroundColor3 = if lockteam then truecolor else falsecolor
end)

predictmovementbutton.Activated:Connect(function()
	predict_movement = not predict_movement
	predictmovementbutton.BackgroundColor3 = if predict_movement then truecolor else falsecolor
end)

npclockbutton.Activated:Connect(function()
	locknpc = not locknpc
	npclockbutton.BackgroundColor3 = if locknpc then truecolor else falsecolor
end)

lockafterdeadbutton.Activated:Connect(function()
	lock_after_dead = not lock_after_dead
	lockafterdeadbutton.BackgroundColor3 = if lock_after_dead then truecolor else falsecolor
end)

detectthroughwallbutton.Activated:Connect(function()
	detect_through_wall = not detect_through_wall
	detectthroughwallbutton.BackgroundColor3 = if detect_through_wall then truecolor else falsecolor
end)

lock_nearest_enemy_button.Activated:Connect(function()
	lock_nearest_enemy = not lock_nearest_enemy
	lock_nearest_enemy_button.BackgroundColor3 = if lock_nearest_enemy then truecolor else falsecolor
end)

local function creat_highlight(adornee, parent)
	local new = Instance.new("Highlight")
	new.Name = "HighlightYOOitAIMHIGHLIGHT"
	new.FillTransparency = .9
	new.FillColor = Color3.new(1, 1, 1)
	new.Adornee = adornee
	new.Parent = parent or highlight_folder
	highlights_store[adornee] = new
	
	adornee.Destroying:Once(function()
		new:Destroy()
		highlights_store[adornee] = nil
	end)
	
end

local function clear_nil_highlight()
	for i,v in pairs(highlights_store) do
		if not v.Adornee then v:Destroy() else continue end
	end
end

show_highlight_plr_folder_button.Activated:Connect(function()
	print("highlight in player folder")
	show_highlight_plr_folder = not show_highlight_plr_folder
	show_highlight_plr_folder_button.BackgroundColor3 = if show_highlight_plr_folder then truecolor else falsecolor

	local hl = if highlights_store[child_table.npc_folder.path] then highlights_store[child_table.plr_folder.path] else nil
	clear_nil_highlight()

	if show_highlight_plr_folder then
		if child_table.plr_folder.path:IsA("Folder") then
			for _,v in pairs(paths_module.get_child_folder("plr_folder")) do
				creat_highlight(v)
			end
			runners.runner_highlight_plr = child_table.plr_folder.path.ChildAdded:Connect(function(child)
				if not highlights_store[child] then creat_highlight(child) end
			end)
		else
			if hl then return end
			creat_highlight(child_table.plr_folder.path)
		end
	else
		if hl then
			hl:Destroy()
		else
			for _,c in pairs(paths_module.get_child_folder("plr_folder")) do
				if highlights_store[c] then highlights_store[c]:Destroy() end
			end
			if runners.runner_highlight_plr then
				runners.runner_highlight_plr:Disconnect()
			end
		end
	end
end)

show_highlight_npc_folder_button.Activated:Connect(function()
	show_highlight_npc_folder = not show_highlight_npc_folder
	show_highlight_npc_folder_button.BackgroundColor3 = if show_highlight_npc_folder then truecolor else falsecolor

	local hl = if highlights_store[child_table.npc_folder.path] then highlights_store[child_table.npc_folder.path] else nil
	clear_nil_highlight()

	if show_highlight_npc_folder then
		if child_table.npc_folder.path:IsA("Folder") then
			runners.runner_highlight_npc = child_table.npc_folder.path.ChildAdded:Connect(function(child)
				if not highlights_store[child] then creat_highlight(child) end
			end)
		else
			if hl then return end
			creat_highlight(child_table.npc_folder.path)
		end
	else
		if hl then
			hl:Destroy()
		else
			for _,c in pairs(paths_module.get_child_folder("npc_folder")) do
				if highlights_store[c] then highlights_store[c]:Destroy() end
			end
			if runners.runner_highlight_npc then
				runners.runner_highlight_npc:Disconnect()
			end
		end
	end

end)

local function make_detect_function()
	if child_table.plr_folder.detect_function then
		child_table.plr_folder.detect_function:Disconnect()
		child_table.plr_folder.childs = {}
		child_table.plr_folder.childs = paths_module.get_child_folder("plr_folder")
	end
	if child_table.npc_folder.detect_function then
		child_table.npc_folder.detect_function:Disconnect()
		child_table.npc_folder.childs = {}
		child_table.npc_folder.childs = paths_module.get_child_folder("npc_folder")
	end

	child_table.plr_folder.detect_function = paths_module.add_detect_function(child_table.plr_folder.path,
		function(child)
			if child:IsA("Model") and child:FindFirstChild("Humanoid") and players:GetPlayerFromCharacter(child) then
				table.insert(child_table.plr_folder.childs, child)
			end
		end,
		function(child)
			local i = table.find(child_table.plr_folder.childs, child)
			if i then
				table.remove(child_table.plr_folder.childs, i)
			end
		end)

	child_table.npc_folder.detect_function = paths_module.add_detect_function(child_table.npc_folder.path,
		function(child)
			if child:IsA("Model") and child:FindFirstChild("Humanoid") and not players:GetPlayerFromCharacter(child) then
				table.insert(child_table.npc_folder.childs, child)
			end
		end,
		function(child)
			local i = table.find(child_table.npc_folder.childs, child)
			if i then
				table.remove(child_table.npc_folder.childs, i)
			end
		end)
end

plr_folder_path.FocusLost:Connect(function(enterPressed)
	local text_parts = string.split(plr_folder_path.Text, ".")
	if text_parts[1] == "Workspace" or text_parts[1] == "workspace" then text_parts[1] = workspace end
	local manual_path = paths_module.safeFind(text_parts[1], nil, text_parts[2], text_parts[3], text_parts[4], text_parts[5], text_parts[6])
	if manual_path then
		-- paths_module.child_table.plr_folder.path = manual_path
		child_table.plr_folder.path = manual_path
		warn("found path for Players: ", child_table.plr_folder.path:GetFullName())
		make_detect_function()
	end
	plr_folder_path.Text = child_table.plr_folder.path:GetFullName()
end)

npc_folder_path.FocusLost:Connect(function(enterPressed)
	local text_parts = string.split(npc_folder_path.Text, ".")
	if text_parts[1] == "Workspace" or text_parts[1] == "workspace" then text_parts[1] = workspace end
	local manual_path = paths_module.safeFind(text_parts[1], nil, text_parts[2], text_parts[3], text_parts[4], text_parts[5], text_parts[6])
	if manual_path then
		-- paths_module.child_table.npc_folder.path = manual_path
		child_table.npc_folder.path = manual_path
		warn("found path for NPC: ", child_table.npc_folder.path:GetFullName())
		make_detect_function()
	end
	npc_folder_path.Text = child_table.npc_folder.path:GetFullName()
end)

child_table.plr_folder.childs = paths_module.get_child_folder("plr_folder")
child_table.npc_folder.childs = paths_module.get_child_folder("npc_folder")

make_detect_function()

runners.runner_main = rs.RenderStepped:Connect(function()
	if not targetenemy 
		or not (targetenemy:IsDescendantOf(child_table.plr_folder.path) or targetenemy:IsDescendantOf(child_table.npc_folder.path))
		or (targetenemy.Humanoid:GetState() == Enum.HumanoidStateType.Dead and not lock_after_dead) then
		targetenemy = nil
	end

	if not enable then return end

	if targetenemy == nil then
		AimLock()
	elseif targetenemy and lock_nearest_enemy then
		AimLock()
	end

end)

runners.runner_mouse = plr_mouse.Move:Connect(function()
	task.spawn(function()
		local x,y = getMousePosition()
		radius.Position = UDim2.new(0, x, 0, y) - UDim2.new(
			radius.Size.X.Scale / 2, radius.Size.X.Offset / 2,
			radius.Size.Y.Scale / 2, radius.Size.Y.Offset / 2
		)
	end)
end)

runners.runner_lockon = rs.RenderStepped:Connect(function()
	if targetenemy and (targetenemy:IsDescendantOf(child_table.plr_folder.path) or targetenemy:IsDescendantOf(child_table.npc_folder.path)) then
		lockon()
	end
end)

runners.runner_input = uis.InputBegan:Connect(function(input, gpe)
	if gpe then return end

	if input.UserInputType == Enum.UserInputType.MouseButton3 then
		enable = not enable
		targetenemy = nil
		if enable then
			colorstatus.BackgroundColor3 = truecolor
		else
			colorstatus.BackgroundColor3 = falsecolor
		end
		radius.BackgroundColor3 = if enable then Color3.fromRGB(255, 255, 0) else colorstatus.BackgroundColor3
	end
end)

print("aimlock: successful run the gui and function")